# SPEC_W33 — Moat & monetization (BINDING)

Base: w33-share master @ 0f7693c (== GitHub main 265cb278, tree 76c9c2e).
All W30/W31/W32 doctrine applies: additive-only schema, hand-written migrations from idx 111, integer cents, deterministic prng, tenant-guarded tRPC (authz green), honest-status vocabulary (integrate for real or fail honestly — no better mocks), paymentConfirm.ts PINNED (md5 2f77ea4816d1adc5cb35473bd35d1697), package.json/lockfiles FROZEN, lazy imports in journey modules, banner/append-only on shared files (`=== W33 <name> ===` … `=== END W33 <name> ===`), env.example W33 banner for new vars, commit+push after every step, rsync to /mnt/agents/output/w33/tree-<x>/, FUSE retry-never-delete (verify file sizes before committing — prior wave had silent empty-blob truncation), logs under /mnt/agents/output/w33/.

Key existing assets to REUSE (read first):
- W31: vendorBills (vendor_bills lifecycle), scheduledPayments (claim-before-send engine), approvals (requireApprovalIfNeeded + executor map), arInvoices (states + reminders)
- W32: installment_plans, recurring_rules, fx_quotes, speed tiers
- Bookkeeping: CSV/PDF export pipeline, digests, Odoo sync outbox
- WhatsApp assistant (ai-agent/ + conversation pipeline), catalog AI patterns (voice/photo→structured)
- escrow settle fee-leg, wallet ledger locked helpers, analytics.revenueTrend (real-query pattern from W30 dashboard honesty)
- kyc/kyb supplier onboarding, INTERNAL_API_KEY service-to-service auth (W30 REQUIRED_BY_ENV), capabilityTokens (W30)
- Scheduler service + k8s CronJobs (40 exist; new routes get next numbers 41+)
- authz scanner: new routers must be tenant-guarded or EXEMPTION_ALLOWLIST with justification

## Coder A — branch w33/tax-statements (migrations 0111-0112, journeys J206-J208)
**Supplier tax profiles + annual statements (Melio W-9/1099 analog).**
- Mig 0111: `supplier_tax_profiles` (id uuid pk, tenant_id, supplier_tenant_id nullable (internal suppliers), vendor_name varchar, vendor_ref varchar (for external vendors without tenant), tax_id varchar(64), tax_id_type varchar(16) [tin|vat|cac|nin|other], country_code char(2), withholding_bps int default 0, verified_at timestamptz null, metadata jsonb, created_at, updated_at) + unique (tenant_id, coalesce(supplier_tenant_id, vendor_ref)). Wire OPTIONAL capture into KYB supplier onboarding + vendor_bills vendor create path (banner edits, keep additive).
- Mig 0112: `annual_statements` (id, tenant_id, supplier ref cols, year int, total_paid_cents bigint, payment_count int, currency, withholding_cents bigint, status varchar(16) [generated|sent|viewed], pdf_path varchar, generated_at, sent_at, unique(tenant,supplier,year)).
- Service supplierTaxStatements: aggregate per-supplier annual totals from REAL payment records (vendor_bill payments + wholesale order payments + payout wallet_tx), integer cents, GROUP BY currency (mixed currency → separate statement rows per currency, never summed across); generate PDF via existing export pipeline (store path, honest `generated` only when file written); send via WhatsApp document push to supplier (status→sent with message id) or dashboard download. Withholding: if profile.withholding_bps>0 compute withheld portion and label `withholding_note` — informational only unless a real withholding rail exists (fail honestly).
- Router `taxStatements` (tenant-guarded; moneyProcedure for generate/send since financial docs): profiles CRUD, generateAnnualStatement(tenantId, supplierRef, year), sendStatement, listStatements.
- J206: profile capture + annual totals match real payments (conservation vs wallet_tx). J207: PDF generated honestly (file exists, status transitions, mixed-currency → 2 statements). J208: send via WA push → sent + viewed on supplier read; regeneration idempotent (unique + replace file).

## Coder B — branch w33/ai-qa-forecast (migration 0113, journeys J209-J211)
**AP/AR AI Q&A + cash-flow calendar & forecast.**
- Mig 0113: `cashflow_forecasts` (id, tenant_id, horizon_days int, generated_at, inflow_cents bigint, outflow_cents bigint, net_cents bigint, currency, shortfall_at date null, detail jsonb, unique(tenant,horizon,date(generated_at)) — or onConflictDoNothing pattern) — stores latest computed snapshot honestly.
- Q&A: extend the WhatsApp assistant with read-only tenant-scoped finance intents (banner edits to existing intent router): "bills due this week", "who do I owe most", "invoice paid?", "expected inflows" — answers computed from vendor_bills/ar_invoices/installment_plans/wallet_tx via a new service `financeQa.ts` (pure query layer, no writes, tenantId from session, never cross-tenant). Deterministic templates for amounts (integer cents → formatted); if the NL layer is unavailable (no LLM key), keyword intent matching MUST still answer the 6 canonical intents (fail honestly, no fabricated numbers — every figure from a real query).
- Forecast service `cashflowForecast.ts`: 30/60/90-day projection from real scheduled outflows (scheduled_payments, recurring_rules next occurrences, installment due dates, vendor_bills due dates) + inflows (expected escrow releases by buyer-confirm deadline, AR invoice due dates, historical paid-invoice velocity as documented heuristic labelled in detail jsonb) → shortfall date if cumulative net < 0; store snapshot (mig 0113) + dashboard procedure `cashflow.forecast(horizon)` returning the computed structure with per-line sources (no fabricated numbers; empty state "No data yet" pattern from W30).
- Cron block `=== W33 forecast ===` weekly snapshot (scheduler allowlist + k8s CronJob #41).
- Router `cashflow` + assistant wiring. J209: Q&A canonical intents answer real figures (seed bills/invoices, assert amounts). J210: forecast math conservation (sum of detail lines == totals; shortfall detected when outflows > inflows + balance). J211: snapshot stored idempotently + dashboard empty-state when tenant has no data.

## Coder C — branch w33/embedded-api (migration 0114, journeys J212-J214)
**Embedded AP-as-a-feature API (Melio's distribution play).**
- Mig 0114: `embedded_clients` (id uuid pk, partner_name varchar, api_key_hash varchar (sha256, never store plaintext), scopes text[] (subset: bills:read bills:write payments:read payments:write invoices:read invoices:write), tenant_id (each client is bound to ONE tenant it serves — partner platforms create per-merchant clients), status varchar(16) [active|suspended], created_by, created_at, last_used_at, unique(api_key_hash)).
- External HTTP surface under `/api/embedded/v1/*` (Express, `_core/index.ts` banner block): auth middleware timing-safe compares sha256(api key) → client, checks status+scope, sets embedded tenant context; rate limit per client (reuse fail-closed checkRateLimit). Endpoints (thin pass-through to EXISTING services — vendorBills, scheduledPayments, arInvoices — never re-implement money logic): GET/POST /bills, GET /bills/:id, POST /bills/:id/pay (goes through the SAME approval gate as in-app — embedded cannot bypass requireApprovalIfNeeded), POST /payments/schedule, GET /payments/:id, GET/POST /invoices. Every mutation writes audit log with actor=`embedded:<clientId>`. All money responses use honest vocab from the underlying services.
- Admin router `embedded` (adminProcedure): createClient (returns ONE-TIME plaintext key, stores hash), suspendClient, rotateKey, listClients (no hashes). env.example W33 banner: `EMBEDDED_API_ENABLED` (default false; boot note that enabling exposes the surface — fail-closed default OFF, 404 when disabled).
- Docs: docs/EMBEDDED_API_W33.md (auth, scopes, endpoints, examples).
- J212: full flow — create client, create bill via API, pay respects approval threshold (embedded pay above threshold → pending_approval, owner approves in-app, poll → paid). J213: scope enforcement (bills:read client cannot POST), suspended client 401, disabled flag 404, rate limit trips. J214: audit rows carry embedded actor; no cross-tenant leakage (client for tenant A gets 404/403 on tenant B resources — resource tenant is derived from the CLIENT binding, never from request params).

## Merger notes (lead)
Merge order A→B→C. Journal idx 111-114 cumulative from 0110 tip. k8s CronJobs: B takes #41 (forecast). routers.ts: taxStatements, cashflow, embedded registrations union by banner. _core/index.ts: B's forecast cron block + C's embedded surface block (distinct banners). Assistant intent router: B only. Journey count 205+9=214 → simulation.test.ts toBe(214). Full gate: tsc 0 (6GB), full unit vitest (server client scripts) 0 failures, 214/214 journeys, authz green, pin intact, lockfiles 0 diff. Then rsync merged-tree and report for publish.
