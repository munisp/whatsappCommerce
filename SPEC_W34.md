# SPEC_W34 — Platform-wide OpenTelemetry + OSS alerting (BINDING)

Base: w34-share master @ e2c0b04 (== GitHub main 0fccb9b1, tree fca3e836).
Decisions (user): backend **Jaeger + Prometheus + Grafana**; alerting **Alertmanager + webhooks** (incl. WhatsApp ops bridge); **tenant.id baggage on all spans + per-tenant RED metrics**.

All doctrine applies: additive-only schema (migrations only if a table is genuinely needed, from idx 115), integer cents, deterministic prng, tenant-guarded tRPC, honest status (telemetry fail-OPEN: collector down must never break requests; platform reports its own telemetry status honestly), paymentConfirm.ts PINNED (md5 2f77ea4816d1adc5cb35473bd35d1697), lazy imports in journeys, banner/append-only on shared files (`=== W34 <name> ===`), env.example W34 banner, commit+push after every step, VERIFY FILE SIZES non-empty before every commit (W32 empty-blob incident), FUSE retry-never-delete, logs under /mnt/agents/output/w34/.

**SANCTIONED EXCEPTION:** package.json + lockfile changes are allowed ONLY for Coder A and ONLY for the pinned OTel/prom-client deps listed below. Everyone else: lockfiles frozen. Rationale: telemetry is this wave's explicit purpose; deps must be exact-pinned, no new transitive tooling.

## Coder A — branch w34/otel-core (journeys J215-J217)
**Node core instrumentation + propagation.**
- New `server/_core/telemetry.ts`: lazy OTel bootstrap (`@opentelemetry/sdk-node`, auto-instrumentations-node: http, express, pg, ioredis, fetch/undici; OTLP trace exporter HTTP + OTLP metric exporter; resource attrs service.name=whatsapp-commerce-platform, service.version from package.json, deployment.environment). Activated when `OTEL_ENABLED=true` (default false — dev/test unchanged); init failure → warn + continue (fail-open, never throw into request path). Also expose `telemetryStatus()` (enabled/exporter reachable/last error) for honest reporting.
- Allowed deps (exact pins, Coder A ONLY): @opentelemetry/api, @opentelemetry/sdk-node, @opentelemetry/auto-instrumentations-node, @opentelemetry/exporter-trace-otlp-http, @opentelemetry/exporter-metrics-otlp-http, @opentelemetry/resources, @opentelemetry/semantic-conventions, prom-client. Add to package.json deps, update BOTH lockfiles (npm + pnpm) so `npm ci`/`pnpm i --frozen-lockfile` pass.
- **tRPC middleware** (`trpc.ts` banner): wrap all procedures — span per procedure `trpc.<router>.<procedure>` with attrs (tenant.id from ctx.session, user.role, error status); metrics: counters/histograms per router.procedure (RED). Never change procedure behavior; telemetry exceptions swallowed + counted.
- **Express**: auto-instrumentation covers inbound; add `x-trace-id` response header (from active span) for support/debugging.
- **Tenant baggage**: middleware extracts tenantId from authenticated ctx (never request params) → ` baggage tenant.id` + span attribute `tenant.id`; helper `withTenantBaggage(ctx, fn)`.
- **Propagation**: inject traceparent/tracestate into (a) internalProcedure service-to-service HTTP calls (X-Internal-Token callers), (b) scheduler service invocations — scheduler.mjs gains an OTel-lite injector: reads `TRACEPARENT` env or generates a root traceparent per cron fire and sends `traceparent` header; server sdk.ts cron path links the span, (c) outbound calls to hermes-bridge / recon-worker / ml-stack / kyc-verifier / odoo / medusa (wrap shared http helpers at ONE choke point — find it; if none, wrap the 5 call sites), (d) TigerBeetle ledger-bridge requests.
- `/api/metrics` (Express, Prometheus text format via prom-client): RED metrics (http_requests_total{route,status_class,tenant_class}, http_request_duration_ms histogram, trpc_procedure_* , escrow_settlements_total{outcome}, payout_latency histogram, cron_runs_total{route,result}, infra_component_up gauge ×15 fed by the existing infraHealth checks — reuse, don't duplicate probes; refresh every 60s in-process), auth required (internal/cron token or admin session), honest 503 when telemetry disabled.
- env.example W34 banner: OTEL_ENABLED, OTEL_EXPORTER_OTLP_ENDPOINT (default http://otel-collector:4318), OTEL_TENANT_METRIC_ALLOWLIST (csv; cardinality guard — tenants not in allowlist collapse to tenant_class="other"; empty = platform-aggregate only), METRICS_TOKEN.
- J215: request produces span with tenant.id baggage + x-trace-id header; metrics endpoint increments. J216: propagation chain — cron fire → server cron route span shares trace_id; internal service call linked. J217: telemetry fail-open — OTEL_ENABLED with unreachable collector: requests succeed, telemetryStatus reports exporter error honestly.

## Coder B — branch w34/otel-stack (journeys J218-J219)
**Observability infra: collector, Jaeger, Prometheus, Grafana, Alertmanager.**
- `deploy/otel/collector-config.yaml`: OTLP receivers (4317/4318), batch processor, memory_limiter, exporters → Jaeger (otlp), Prometheus (spanmetrics connector → RED metrics from spans; prometheus exporter :9464), logging exporter at warn. Tail-sampling: keep 100% errors, 10% baseline ok (documented).
- docker-compose banner block: `otel-collector`, `jaeger` (all-in-one, 16686 UI), `prometheus` (scrape: platform /api/metrics w/ METRICS_TOKEN bearer, collector :9464, alertmanager), `grafana` (provisioned datasources: Prometheus+Jaeger; provisioned dashboards), `alertmanager`. Healthchecks + honest depends_on. k8s: `k8s/otel-stack.yaml` (collector DaemonSet-or-Deployment, jaeger, prometheus, grafana, alertmanager + configmaps) + kustomization.
- Grafana dashboards (provisioning JSON, committed): "Platform RED" (rate/errors/duration per route + per router.procedure), "Infra components" (infra_component_up ×15 + latency), "Money rails" (escrow settlements outcome, payout latency p50/p95, cron failures), "Tenant explorer" (per-tenant RED for allowlisted tenants, template var). Low-saturation palette, no Google-style design.
- `deploy/otel/alert-rules.yml` (Prometheus): ReadinessFlapping (health/ready 503s), Elevated5xx (rate >5% 5m), CronFailure (cron_runs_total{result=error} >0 15m), EscrowSettleFailure (escrow_settlements_total{outcome=error}), PayoutLatencyHigh (p95 > 30s), TenantErrorSpike (per-tenant error rate >3σ vs its own 1h baseline, allowlist-only), ComponentDown (infra_component_up==0 5m), plus honest test-rule comments. Alertmanager config: receivers email/webhook; severity routing (critical→webhook+email, warning→email); inhibition rules.
- **WhatsApp ops bridge**: `deploy/otel/alertmanager-wa-bridge.mjs` small service: receives Alertmanager webhook → formats alert → sends via platform's existing WhatsApp send pipeline to OPS_ALERT_WHATSAPP number (internal-authenticated call). Fail-open (alert loss logged, never crash loop; /health endpoint). Compose+k8s service. env: ALERTMANAGER_WA_BRIDGE_ENABLED (default false), OPS_ALERT_WHATSAPP.
- DeployChecklist + docs/OBSERVABILITY_W34.md (architecture, how to open Jaeger/Grafana, alert catalog, cardinality guidance).
- J218: compose config validation — collector/jaeger/prometheus/grafana/alertmanager services present with healthchecks, alert rules YAML parses (all rules valid promql via a light parser check), grafana provisioning files valid JSON. J219: WA bridge unit path — alertmanager webhook payload → formatted message → platform send invoked (mock), fail-open on send error, disabled-default honest 404.

## Coder C — branch w34/otel-sidecars (journeys J220-J221)
**Python sidecars + tenant cardinality + dashboards-for-tenants glue.**
- kyc-verifier (python): `opentelemetry-sdk` + `opentelemetry-exporter-otlp` + flask/fastapi instrumentation (match its framework; add to its requirements with exact pins; Dockerfile unchanged except env passthrough). OTEL_ENABLED default false; /health echoes telemetry status honestly alongside vlm_mock_mode (W30 pattern). Trace context extracted from inbound traceparent (C's A-to-sidecar calls continue the trace).
- ai-agent (python): same pattern if framework-compatible; if its framework lacks auto-instrumentation, wrap its main request handler manually with spans (honest: manual spans named ai.agent.handle with tenant attr) — never fake depth.
- Go/Rust services: inspect services/ — if they have buildable mains, add OTel via their SDKs ONLY if it doesn't require a toolchain unavailable in CI; otherwise document honestly in OBSERVABILITY doc which services emit traces and which don't (fail honestly > better mock).
- **Tenant cardinality guard service** (`server/services/telemetryCardinality.ts` — coordinate with A's allowlist): helper `tenantMetricClass(tenantId)` (allowlisted → tenantId label, else "other"), admin router `telemetry` (adminProcedure): getStatus (telemetryStatus + collector reachability + allowlist size), setTenantAllowlist (persisted — if a table is needed: migration 0115 `telemetry_tenant_allowlist` (tenant_id pk, added_by, created_at); else platform config row — your call, keep additive).
- Per-tenant Grafana dashboard "Tenant 360" (provisioned): traces-link by tenant.id, RED for that tenant, alert history placeholder (honest "queried from Alertmanager API" if wired, else section omitted).
- J220: python sidecar traceparent extraction — kyc-verifier called with traceparent header continues trace (assert via its debug echo or exported span attrs in test mode), OTEL disabled default unchanged, /health reports telemetry status. J221: cardinality guard — non-allowlisted tenant collapses to "other", allowlist admin ops audited, /api/metrics labels bounded (assert label cardinality ≤ allowlist+N under multi-tenant load in sim).

## Merger notes (lead)
Merge order A→B→C. A owns package.json/lockfiles (ONLY A). Shared: _core/index.ts (A: /api/metrics + x-trace-id; B: none; C: none), env.ts (A: OTEL env parsing), routers.ts (C: telemetry admin router), compose (B only), k8s (B only), runner.ts J215-221 banners, simulation.test.ts → toBe(221). Journal only if C adds 0115. Full gate: tsc 0 (6GB), full unit vitest 0 failures, 221/221 journeys, authz green, pin intact, `npm ci` AND `pnpm install --frozen-lockfile` both pass with A's updated lockfiles. Rsync merged-tree, report for publish.
