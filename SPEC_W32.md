# SPEC_W32 — Cash-flow differentiation (BINDING)

Base: w32-share master @ 4985e73 (== GitHub main adacce28, tree ad0db6e2).
All W30/W31 doctrine applies: additive-only schema, hand-written migrations from idx 106, integer cents, deterministic prng, tenant-guarded tRPC (authz green), honest-status vocabulary (integrate for real or fail honestly — no better mocks), paymentConfirm.ts PINNED (md5 2f77ea4816d1adc5cb35473bd35d1697), package.json/lockfiles FROZEN, lazy imports in journey modules, banner/append-only on shared files, env.example W32 banner for new vars, commit+push after every step, rsync to /mnt/agents/output/w32/tree-<x>/, FUSE retry-never-delete, logs under /mnt/agents/output/w32/.

Key existing assets to REUSE (read them first):
- tradeCredit: creditScore.getMerchantScore, microLoans (locked acceptLoanTx disbursement, funding leg via credit_facilities), capture.ts (mandate repayment, deterministic refs), creditAccounts/termsDays
- W31: vendorBills (recordVendorBillPayment locked debit, approval gate), scheduledPayments (claim-before-send engine, batchPay), approvals (requireApprovalIfNeeded + executor map)
- escrow settle fee-leg pattern (platform fee deduction in settleEscrowAtomic), wallet ledger locked helpers, providers (Paystack/Flutterwave) + initiateWithFallback, metaMock
- Scheduler service + k8s CronJobs (36-38 exist; new routes get next numbers)

## Coder A — branch w32/pay-over-time (migration 0106, journeys J198-J199)
**Pay Over Time (installment bill pay).** Melio flagship; our version funded by the trade-credit stack.
- Migration 0106: `installment_plans` (id uuid pk, tenant_id, vendor_bill_id, principal_cents bigint, installments int, fee_bps int (config), per_installment_cents bigint, currency, status varchar(16) [active|repaid|defaulted|cancelled], loan_id (links merchant_loans), schedule jsonb [{seq, dueAt, amountCents, status, paidAt}], created_at, updated_at) + index (tenant_id,status).
- Flow: vendorBills.recordPayment gets opt-in `payOverTime: {installments: 3|6|12}` — eligibility via creditScore.getMerchantScore (min threshold from platform config) + requireApprovedKyb (existing); vendor paid IN FULL today via microLoans-style disbursement: locked funding leg (credit_facilities FOR UPDATE decrement, merchant_loan_funding row, TigerBeetle transfer via ledgerBridgeRequest, wallet untouched by principal — the bill's paid_cents goes straight to full with payment_ref `pot:<planId>`), loan created with outstanding = principal + fee (integer-cent fee, fee leg to platform wallet on each repayment, mirroring escrow fee pattern); bill status `paid` honestly (vendor WAS paid) with metadata.financing="pay_over_time".
- Repayment: installments collected via existing mandate capture (capture.ts) on the installment due dates — reuse, don't rebuild; schedule stored on the plan; a cron block `=== W32 installment due ===` (scheduler allowlist + k8s next number) triggers due-installment capture; failed capture → honest overdue installment, dunning via WA, default path reuses microLoans late/default handling.
- Cancel/early-settle: repay remaining principal early (single payment, fee recalculated honestly — fee on elapsed period only if config allows, else full; document choice).
- Honest vocab: merchant sees "Vendor paid in full · you're repaying ₦X in N installments"; never imply the vendor waits.
- J198: eligible merchant pays bill over 3 installments → vendor bill paid in full immediately (funding leg + facility decrement + TB transfer asserted via metaMock ledger.calls) → installments captured on schedule → plan repaid. J199: ineligible score → honest rejection (bill stays pending, nothing moves); failed installment capture → overdue installment + WA dunning; early settle.

## Coder B — branch w32/recurring-tiers (migrations 0107-0108, journeys J200-J202)
**Recurring bills/auto-pay + payout speed tiers.**
- Migration 0107: `recurring_rules` (id uuid pk, tenant_id, kind [vendor_bill|adhoc], recipient/vendor jsonb, amount_cents, currency, cadence varchar(16) [weekly|monthly], day_of_month int, auto_pay_under_cents bigint default 0, next_run_at, status [active|paused|cancelled], last_run_at, created_by, created_at, updated_at) + index (status,next_run_at).
- Recurring engine: cron block `=== W32 recurring ===` (allowlist + k8s next number, daily): due rules → create vendor_bills row (capture_source='recurring' — additive status/source vocab) or adhoc scheduled_payment with execute_at=now; if amount <= auto_pay_under_cents AND approvals policy satisfied → auto-execute via scheduledPayments engine (idempotency `recur:<ruleId>:<period>`); else WA request-for-approval (one-tap approve links into W31 approvals). Advance next_run_at atomically with creation (same tx) so a crash can't double-create.
- Migration 0108: payout speed tiers — extend scheduled_payments/withdrawal path: `speed varchar(16) default 'standard'` [standard|instant] on scheduled_payments (ALTER ADD COLUMN, additive) + platform config `instant_payout_fee_bps`; instant → execute immediately (claim+execute inline in schedule call when execute_at<=now), fee leg: wallet_tx fee row to platform wallet (reference `schedfee:<id>`, integer cents, mirrors escrow fee leg) deducted from the same locked debit; standard stays free T+1 (honest copy: "arrives next business day" only if that rail exists — else honest "processed in the next batch").
- Router surfaces: recurringRules CRUD (moneyProcedure mutations), scheduledPayments.schedule accepts speed.
- J200: monthly recurring supplier bill → cron creates bill + auto-pays under threshold → next_run advanced once (replay no-op); J201: above auto-pay threshold → WA approval request → approve executes; pause/cancel stop future runs honestly. J202: instant payout → immediate execution + fee leg to platform wallet asserted (fee+net==gross); standard → waits for tick.

## Coder C — branch w32/earlypay-fx (migrations 0109-0110, journeys J203-J205)
**Early-payment discounts (wholesale) + cross-border FX vendor payouts.**
- Migration 0109: wholesale PO terms — add to wholesale orders (or new `po_payment_terms`): discount_bps, discount_window_days, due_date; computed `early_pay_deadline`. Surface on buyer's PO view: "Pay by <date> to save ₦X" (server-derived math, integer cents). earlyPay procedure: pays PO early via existing wholesale payment rails with discount applied — the ORDER's payable amount is reduced only through a guarded conditional UPDATE (status + discount_applied claim-first so a double-tap never double-discounts); supplier receives full invoiced-minus-discount honestly (their configured terms); ledger + wallet reflect the discounted amount; late path unchanged.
- Migration 0110: `fx_quotes` (id uuid pk, tenant_id, from_currency, to_currency, amount_cents, rate numeric(20,8), fee_bps, fee_cents, total_cents, provider varchar(24), provider_ref unique, status [quoted|accepted|expired|executed|failed], expires_at, created_at, accepted_at) + index (tenant_id,status).
- FX payout flow (new router section `fxPayouts`): quote → accept (within expiry, guarded single consume) → execute: debit wallet in from_currency via locked helper, credit/deliver via Mojaloop rail (existing mojaloop service) or honest UNAVAILABLE when no live corridor (fail honestly per doctrine); executed quote + payout reference; expired quotes refuse; rate from a pluggable rate source with deterministic sim fixture (shared/prng.ts or fixed table) and honest metadata.source.
- J203: early pay within window → discounted debit + supplier credited honestly + double-early-pay CONFLICT; after window → discount honestly unavailable. J204: FX quote → accept → executed with exact fee math (fee+net==gross) + expired quote refusal + replay acceptance no-op. J205: no-corridor currency → honest UNAVAILABLE, nothing moves.

## Merger notes
Order A → B → C. Journal idx 106-110 cumulative from 0105 tip (re-chain + cumulative snapshots — W31 lesson: rebuild by applying per-branch deltas onto the running chain; NEVER line-union schema.ts/_core/index.ts — splice whole banner sections verbatim). runner.ts union by banner; simulation.test.ts count = 197 + 8 = 205 (toBe(205)). _core/index.ts: A owns installment-due cron block, B owns recurring cron block — splice banner sections. escrow.ts/logistics untouched. vendorBills.ts: A adds payOverTime option + B adds capture_source='recurring' vocab usage (no schema conflict). scheduledPayments.ts: B adds speed column + instant path. Full gate: tsc 0 (6GB), full vitest excl sim, 205/205 journeys, authz green, pin intact, lockfiles clean. Push master to w32-share, rsync /mnt/agents/output/w32/merged-tree/, lead publishes (BASE_SHA adacce28d6bed16db3085aece0fdb680aafcbb5f, branch wave32-cashflow).
