# SPEC_W45 — Residual P0s + pipeline/money/orders P1s + Go/Rust services (BINDING)

Source: /mnt/agents/output/w45/residual-backlog.md (verified residual backlog; each row has current file:line evidence + fix sketch — READ IT). Base: main @ 226b132.

7 coders, non-overlapping. Journeys J352+; each coder owns 5. simulation.test.ts count is MERGER-owned.

## Coder A1 — w45/webhook-core — J352–J356 — owns server/_core/index.ts webhook region EXCLUSIVELY
MSG-3 (reject/quarantine unknown phone_number_id, ops alert, never default-tenant in prod — also retry heartbeat), MSG-4 (iterate ALL entry[]/changes[], full fan-out in waWebhookEvents), MSG-5 (human_active suppresses bot replies, persist inbound, agent release-to-bot), MSG-7 (per-message try/catch → processed/failed+lastError so heartbeat works), MSG-8 (retry heartbeat re-dispatches through the SAME per-message pipeline with claimWebhookEvent, all types not just text), MSG-12 (system customer_changed_number: migrate identity/consent/session/cart keys to new wa_id), MSG-13 (order/button/contacts/sticker/unsupported → catalog cart / interactive dispatch / polite fallback), MSG-14 (conv cache keyed conv:{tenantId}:{phone}), MSG-16 (PO approval reply: unique/exact token match, reject >1, verify sender=adminPhone, remove dead query), MSG-21 (claim wamid before onboarding-intake branch), MSG-22 (release claim or record pending+replay on over-quota). No migrations unless essential (range 0147 if needed).

## Coder A2 — w45/messaging-services — J357–J361 — owns waSender.ts, cartRecovery.ts, new service files
MSG-6 (async mirror inbound media incl. audio to object storage at webhook time via existing storagePut; store internal key; Graph id as fallback — seam call from index.ts must be a small exported helper A1 can call), MSG-9 (monotonic status: sent<delivered<read, failed terminal; compare statusTimestamps before scalar overwrite), MSG-10 (parse errors[].code on failed receipts → per-tenant suppression list; consult in broadcast/cartRecovery/runWaSendRetries), MSG-11 (cart recovery gate on getWindow; outside window → approved template or skip), MSG-24 (terminal catch → localized "couldn't process that photo" reply), MSG-25 (per-phone_number_id ban/401/403/131026 circuit breaker → stop tenant sends, alert via email/dashboard NOT the banned number). New tables if needed: range 0148 (suppression list should be Redis + PG row — your call, document). COORDINATION: you export helpers; A1 owns index.ts edits — document exact call sites + signatures in your report for merger.

## Coder B1 — w45/money-scheduled — J362–J366 — owns scheduledPayments.ts, approvals.ts, routers/sla.ts
PAY-10 (schedule-time amount==remaining validation; remove/fix syncVendorBillBestEffort blanket-set fallback), PAY-11 (stale-claim reaper: claimed && updatedAt < now-10min → pending, in the tick), PAY-12 (approval expiry ALSO fails/cancels the parked payment — approval_expired — release recurring period; fix the W32 re-park guard), PAY-21 (merchantResponseDeadline sweep: escalate past deadline, auto-resolve buyer-favour after grace — config-gated + audited), PAY-22 (stale-escrow queue: buyer "confirm or dispute" prompt both channels, ops alert, merchant-no-ship auto-refund path). New cron routes must use W42 cronAuth scope+jti + scheduler.mjs allowlist (J178 contract). No migrations unless essential (range 0149).

## Coder B2 — w45/money-intents — J367–J371 — owns routers/payment.ts, initiateWithFallback.ts, services/payments/*, new quarantine service
PAY-13 (paymentMismatchQuarantine table — migration 0150 — + ops alert + auto-refund via executeProviderRefund; paymentConfirm.ts is PINNED md5 2f77ea4816d1adc5cb35473bd35d1697 — adjacent seam ONLY), PAY-15 (replay compares amount/currency to current order total; cancel+remint intent when changed), PAY-23 (ISO-4217 exponent table for minor-unit conversions; XOF zero-decimal; dust policy: carry remainder + min-payout floor), PAY-24 (cron sweep: old non-terminal transfers → verifyTransfer; failed/not-found → compensating credit; stale otp → alert + auto-cancel), PAY-25 (fallback only on definitive failure OR verify/void provider A first; duplicate-completed-payment detector → auto-refund + alert).

## Coder B3 — w45/money-ledger — J372–J376 — owns fxPayouts.ts, payOverTime.ts, ledgerBridge.ts, mandates.ts
PAY-16 (deterministic transferId from quoteId; post-commit outbox worker for Mojaloop leg; fulfil/error callback + poller), PAY-17 (refuse debit/credit where currency != wallet.currency; fail-closed), PAY-18 (post-commit outbox for TigerBeetle legs OR PG↔TB balance recon job for credit-facility/mandate accounts), PAY-19 (platform-fees:${plan.currency} + per-currency fee accounts), PAY-20 (mandate revocation → pause auto-capture + notify with re-link CTA + manual payment-link fallback + admin cancel/restructure; no infinite dunning). Outbox table if needed: migration 0151.

## Coder C — w45/orders-p0 — J377–J381 — owns logistics.ts, escrowLifecycle.ts, poFlow.ts, orderCrud.ts buyer-cancel, routers/escrow.ts setConfig region ONLY
**P0s FIRST:** ORD-7 (failed/returned delivery → pause escrow auto-release + redelivery task; terminal RTS → restock committed + refund path; buyer-protection clock pauses), ORD-17 (goods_receipts table — migration 0143 — per PO line receivedQty + vendor_bills.poId — 0144 — match check billed ≤ received before payment release), ORD-18 (markPoFulfilled/receipt → map productRef→product + credit stockQuantity in same txn + stock_adjustments audit row via W43 recordStockAdjustment). Then P1s: ORD-9 (products.weightKg + sum order weight at checkout; tenant deliveryZones from geo.ts instead of LAGOS_HINTS), ORD-26 (post-sync Odoo reconciliation job comparing inventory_snapshots vs products ledger, alert on drift), ORD-27 (buyer "cancel order" chat intent both channels → guarded pre-ship cancel via W38 cancelOrder incl. escrow refund), PLT-12 (delivery-PIN cap atomic: Redis INCR or single UPDATE ... WHERE count<limit), PLT-13 (bankApiBaseUrl: z.string().url() + ssrfGuard at write AND call time + audit log).

## Coder D — w45/go-rust-services — J382–J386 — owns services/* Go/Rust only
MSG-15 (hermes-bridge PO approval: approved template w/ buttons outside 24h window; persist pendingApprovals in PG/Redis w/ expiry + reminder sweep — kill sync.Map), MSG-17 (conversation-orchestrator: real Chatwoot API reply + resolve→bot_active path, OR delete the service — decide honestly, document), MSG-18 (message-processor: real rdkafka consumer loop + Kafka DLQ topic, OR drop from architecture honestly), MSG-19 (notification-service: implement queue-backed pipeline OR remove), MSG-20 (hermes-bridge real Kafka consumer + configurable public callback base URL, no localhost hardcode). NO-MOCK DOCTRINE: either wire it for real or remove it and update docs/architecture — never a better mock. Go/Rust toolchains may be absent — if so, keep changes compilable-by-inspection, write tests, flag as compile-unverified. If you remove a service: update k8s/, docs, and any references.

## Cross-cutting invariants (ALL coders)
- Additive-only schema; hand-written migrations; journal idx+prevId chained from 0142 tip; cumulative snapshots full column union. Assigned migration ranges: C 0143–0144, A1 0147, A2 0148, B1 0149, B2 0150, B3 0151 — use ONLY if you actually need a migration.
- Integer cents; claim-first FOR UPDATE; idempotency keys; verify-before-compensate on PSP timeout; paymentConfirm.ts md5 2f77ea4816d1adc5cb35473bd35d1697 PINNED (adjacent seams only).
- Channel parity BOTH WhatsApp+Telegram via channelSender/notifyCustomer; register new categories (J246 subset semantics, no hard-coded counts). Banner `// === W45 <topic> ===`.
- tenantId scoping + assertTenantActive on state mutations; Redis counters shared-safe (W42 patterns); cron routes W42 cronAuth scope+jti + scheduler allowlist.
- No TEMP STUBs, no empty files, lockfiles untouched, no new deps without flagging.
- Gates before push: tsc 0 (NODE_OPTIONS=--max-old-space-size=8192), your 5 journeys N/N, pin md5, lockfiles, no empties. PUSH INCREMENTALLY every ~30 min (/home wipes). Push branch to /mnt/agents/output/w45-share + ls-remote verify. Fast-fail after 2 attempts — report, don't grind. Budget discipline: implement, gate your journeys, push — do NOT run the full suite (merger does).
