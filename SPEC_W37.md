# SPEC_W37 — Telegram channel with WhatsApp parity

Base: main @ 18ed3f3b7c34741d67a340b9a131cf3fd8d43014. BINDING. Read /mnt/agents/output/w37/wa-surface-map.md first — it is the authoritative map.

## Global invariants (all coders)
- paymentConfirm.ts PINNED (md5 2f77ea4816d1adc5cb35473bd35d1697).
- package.json/lockfiles FROZEN — Telegram Bot API is plain HTTPS JSON; use existing fetch/undici. NO new deps.
- Additive-only schema; hand-written migrations only (Coders A/B share 0117/0118 as assigned); journal prevId re-chain at merge.
- Banner edits (`// === W37 telegram ===` … END) on shared files; append-only where possible.
- Telegram DISABLED by default (TELEGRAM_ENABLED env, default false); fail-open everywhere; honest status in /health/ready + infra probes.
- Env additions → env.example.txt under `=== W37 ===` banner (Coder B owns).
- Channel doctrine: NO regression to WhatsApp behavior. Every shared-file change must leave WA paths byte-equivalent in behavior. Telegram parity is additive routing, not a rewrite of nlp.ts.
- Journeys J231+ as assigned; merger owns final count in simulation.test.ts.
- Telegram identity: chat_id is the session key. Phone linkage ONLY via explicit Telegram contact-share (request_contact keyboard) — never infer. Model: `telegram:<chat_id>` as channel-scoped identity wherever waPhoneNumber keys sessions (normalize via helper, don't string-concat ad hoc).
- No empty files; verify wc -c.

## Coder A — outbound: telegramSender + channelSender facade
Owns: server/services/telegramSender.ts (NEW), server/services/channelSender.ts (NEW), server/services/waSender.ts (banner-only seams if needed), migration 0117, journeys J231–J234.
1. telegramSender.ts: Bot API client mirroring waSender semantics: sendTelegramText(chatId, text, {parseMode HTML, disablePreview}), sendTelegramKeyboard(chatId, text, buttons[] -> inline_keyboard with callback_data carrying the EXISTING button id grammar `menu_<n>`, `order_*`, `catalog_ai:*` — do not invent new ids), sendTelegramList (long lists -> chunked keyboards + "More" pagination keyboard), sendTelegramMedia(chatId, type photo|document|voice, bufferOrUrl, caption), sendTelegramLocationRequest (keyboard button request_location), sendTelegramContactRequest (request_contact), answerCallbackQuery (ack within 10s, alert text optional), editMessageReplyMarkup (clear keyboards after tap to prevent double-tap), sendChatAction(typing). Config per tenant: bot token from tenant settings (encrypted v1: same scheme as whatsapp.accessToken — reuse the same encrypt/decrypt helpers, do NOT roll new crypto). Base URL https://api.telegram.org/bot<token>; file download via getFile -> api.telegram.org/file/bot<token>/<path>.
2. Retry/DLQ parity: reuse the waSender retry infrastructure pattern — classified errors (429 retry_after honored, 5xx retry, 4xx dead-letter), backoff 1m/5m/15m/1h, 4 attempts, dead-letter admin alert row in the SAME dlq table waSender uses IF it has a channel column or accepts generic rows — else create telegram rows in channelMessages with failed status + admin alert via existing alert path. Honest choice documented.
3. channelSender.ts: facade `sendChannelMessage(tenant, channel, to, payload)` routing whatsapp->waSender / telegram->telegramSender; capability matrix export (e.g. supportsTemplates, supportsButtons, maxButtonsPerRow) so callers degrade honestly; status report fn for probes. Do NOT refactor waSender internals.
4. Migration 0117 `telegram_outbox` (id, tenant_id, chat_id, kind, payload jsonb, status, attempts, next_retry_at, last_error, created/updated) + tenant settings.telegram doc column if needed (additive jsonb key preferred over new column if settings is jsonb — check schema.ts first). Journal follows 0116 pattern.
5. Journeys: J231 telegramSender builds correct Bot API payloads (mock fetch: text/keyboard/list-chunking/media/location+contact request); J232 retry classification incl. 429 retry_after + dead-letter after 4; J233 channelSender routing + capability matrix honest degrade; J234 encrypted token round-trip reusing existing crypto helpers.
Report: file list, design decisions, journey results, branch w37/telegram-outbound.

## Coder B — inbound: Telegram webhook + identity + config + NLP normalization
Owns: server/routes or server/_core/index.ts banner (webhook route), server/services/telegramInbound.ts (NEW), server/services/channelIdentity.ts (NEW), tenant.ts router banner (config get/update), consent.ts (banner seam), migration 0118, env.example.txt `=== W37 ===` banner, journeys J235–J239.
1. Route `POST /api/webhooks/telegram/:tenantId` (banner block next to WA webhook): header `X-Telegram-Bot-Api-Secret-Token` validated against per-tenant stored secret (fail-closed, timing-safe); immediate 200 ack then inline processing mirroring WA flow; dedupe via processed_webhook_events (namespace key `tg:<update_id>` — check webhookDedupe accepts namespaced ids; banner seam if needed); tenant = path tenantId verified to HAVE telegram enabled (never first-match by token — avoids TEN-3 class bug; also add UNIQUE guard approach for bot username in config update).
2. Update normalization (telegramInbound.ts): message.text -> canonical text event; callback_query -> synthesize the SAME interactive-reply payload WA buttons produce (id = callback_data) + answerCallbackQuery ack; message.contact -> bind chat_id<->phone in channelIdentity (only self-shared: verify contact.user_id == from.id); message.location -> same location event WA path emits; voice/photo/document -> download via getFile then hand to the SAME media pipeline entry points (transcribe.handleInboundVoiceNote, visual-search/receipt chain) behind TELEGRAM_MEDIA_ENABLED; /start -> opt-in consent event; /stop + "STOP" text -> consent revocation via consent.ts seam (Telegram implements STOP correctly from day one).
3. channelIdentity.ts: resolve(tenantId, channel, channelScopedId) -> canonical customer identity used by nlp sessions; session key normalization helper `sessionKeyFor(channel, id)` (wa: E.164 as today; tg: `telegram:<chat_id>`); wire into nlp.ts via MINIMAL banner seam (a resolver call at session-key computation; default WA path unchanged).
4. tenant.ts banner: getTelegramConfig/updateTelegramConfig (masked token display same as WA), fields: botToken(encrypted), botUsername, webhookSecret, enabled; validation: token format, uniqueness check on botUsername across tenants with honest conflict error.
5. Migration 0118 `telegram_identities` (tenant_id, chat_id, phone_e164 nullable, username, linked_via text, created/updated, uniq tenant+chat) + consents.channel usage already supported (channelEnum has telegram — verify, else additive enum alter). Journal re-chain from 0117 at merge (merger owns).
6. env.example W37 banner: TELEGRAM_ENABLED, TELEGRAM_MEDIA_ENABLED, TELEGRAM_WEBHOOK_BASE (for setWebhook helper docs).
7. Journeys: J235 webhook secret fail-closed + ack-then-process + dedupe namespace; J236 callback_query -> WA-equivalent interactive payload (id grammar preserved) + keyboard cleared; J237 contact-share binding incl. rejecting contact.user_id != from.id; J238 /start and /stop consent events (revocation honored on subsequent inbound); J239 session-key normalization (tg identity isolated from wa identity of same phone).
Report: file list, decisions, journey results, branch w37/telegram-inbound.

## Coder C — caller parity + parity matrix + journeys
Owns: caller migrations (banner seams only, one mechanical pattern), server/services/channelParity.ts (NEW), broadcast/cartRecovery/dunning/escalation/logistics-PIN/receipts/order-notifs/approvals seams, journeys J240–J246.
1. channelParity.ts: per the scout's 15-category checklist — a routing helper `notifyCustomer(tenant, customerRef, category, payload)` that resolves channel from conversation/customer identity and delegates to channelSender; category adapters for: order confirmations/status, payment receipts, payment links (telegram: deep-link button URL instead of wa.me), delivery PIN message, PO approval notify (merchant-side), cart-abandonment nudge, broadcast campaign message, dunning reminder, escalation/handoff notice, financeQa reply, low-stock/ops merchant alerts, annual statement doc, installment receipt, refund notice, delivery status updates.
2. Migrate callers MECHANICALLY: where a caller has a telegram-capable conversation (channel=='telegram' or identity resolves to telegram), route via notifyCustomer; else unchanged WA path. Banner each seam. Priority order: orderCrud notifs, logistics.ts delivery PIN + status, payment receipts (adjacent to paymentConfirm — READ ONLY that file, seam in callers), cartRecovery, broadcast, dunning, escalation, financeQa, approvals/PO notify. Broadcast: telegram recipients chunked 30 msg/sec (Bot API limit) — honest throttle.
3. 24h window: WA concept; telegram has none — parity.ts must document + expose `requiresSessionWindow(channel)` so cron nudges don't wrongly suppress telegram sends (check cartRecovery gating).
4. Journeys: J240 order confirmation renders on both channels with same semantic content; J241 delivery PIN via telegram keyboard flow; J242 payment-link message uses URL button (not wa.me) on telegram; J243 broadcast mixed-channel fan-out with telegram throttle honored; J244 cart nudge not suppressed on telegram (no 24h window) but still suppressed within WA window; J245 escalation notice parity; J246 parity matrix covers all 15 categories (snapshot test of category registry vs checklist).
Report: callers migrated (list), any category left WA-only (must be justified: genuinely channel-specific), journey results, branch w37/telegram-parity.

## Merger (reuse warm coder)
Order A → B → C. Seams: consent.ts banner conflicts (append both), nlp.ts resolver seam vs C's usage, migration journal 0116->0117->0118 re-chain, env.example single W37 banner merge, simulation.test.ts count = actual journey files (expect 246: 230 + J231–J246). Full gate: tsc 0; vitest full 0 failures; sim N/N with TELEGRAM_ENABLED unset AND a telegram-enabled sim pass for the new journeys; authz green; pin md5 intact; lockfiles UNCHANGED; no empty files; no conflict markers. Deliver /mnt/agents/output/w37/merge-log.md + merged tree rsync to /mnt/agents/output/w37/merged-tree.
