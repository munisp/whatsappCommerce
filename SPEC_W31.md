# SPEC_W31 — Melio-inspired AP/AR fundamentals (BINDING)

Base: w31-share master @ c03d878 (== GitHub main 3f7a0c02, tree 3dedcbc7).
Doctrine: additive-only schema, hand-written migrations from idx 100 (0100…), integer cents, deterministic (shared/prng.ts), tenant-guarded tRPC (authz scanner must stay green; EXEMPTION_ALLOWLIST only), honest-status vocabulary (never claim money moved when it didn't; integrate for real or fail honestly), paymentConfirm.ts PINNED (md5 2f77ea4816d1adc5cb35473bd35d1697), package.json/lockfiles FROZEN, journey modules lazy-import server services inside run(), shared-file banner/append-only protocol, env.example.txt W31 banner for new env vars, commit+push after every step, rsync tree to /mnt/agents/output/w31/tree-<x>/ (excl .git/node_modules). FUSE: never delete refs, retry transient errors, rm -f .git/index.lock on add failures, logs under /mnt/agents/output/w31/.

Reuse before building: atomic wallet ledger helpers in escrow.ts walletRouter section (post-W30: FOR UPDATE + conditional decrements + unique-ref idempotency), refunds/payments provider layer, stepUp.ts, moneyProcedure/assertMoneyAccess (trpc.ts), scheduler service patterns (claim-before-send, odoo_sync_outbox exactly-once), expense OCR pipeline (bookkeeping), OdooAdapter vendor-bill sync (W28 odooSync), invoice router (commission invoices), WA assistant/notification helpers, metaMock for PSP sim.

## Coder A — branch w31/vendor-bills (migrations 0100-0101, journeys J183-J186)
**Vendor Bills AP inbox.** New router server/routers/vendorBills.ts:
- Migration 0100: `vendor_bills` (id uuid pk, tenant_id, vendor_name, vendor_contact jsonb, bill_number, description, amount_cents bigint, currency varchar(3), issue_date, due_date, status varchar(16) default 'pending' [pending|scheduled|approved|paid|partially_paid|overdue|cancelled], paid_cents bigint default 0, capture_source varchar(16) [photo|pdf|whatsapp|manual|odoo], capture_media_key, ocr_confidence numeric, ocr_raw jsonb, payment_ref unique, approval_id, odoo_sync_state, created_by, created_at, updated_at) + index (tenant_id,status) + index (tenant_id,due_date). Migration 0101: `vendor_bill_events` (audit: bill_id, event, actor, metadata jsonb, created_at).
- Procedures: create (manual or capture: accept media key from existing storage, run existing OCR extraction; store ocr_confidence; low confidence → fields flagged for review, honest), list (filters status/due window/vendor), get, update (pre-payment only), cancel, markOverdue sweep (cron), recordPayment — pays via REAL wallet debit using the post-W30 locked helpers (tenant wallet FOR UPDATE, conditional decrement, unique payment_ref idempotency, wallet_tx reference `vbill:<billId>`); partial payments accumulate paid_cents with honest partially_paid; insufficient balance → honest INSUFFICIENT_FUNDS, nothing moves. All moneyProcedure-guarded (owner|operator) for payment actions; analyst read-only.
- WhatsApp capture: WA assistant command/forward path — merchant forwards a supplier invoice image in chat → vendor_bills row with capture_source='whatsapp' + confirmation reply with extracted fields. Wire into the existing assistant intake (lazy imports).
- Odoo hook: on paid, enqueue odoo_sync_outbox entry (vendor bill with payment) reusing W28 adapter — if Odoo not configured, outbox row stays queued honestly (no fake success).
- Journeys: J183 manual bill → pay in full (wallet debit asserted, Odoo outbox row); J184 partial payments → partially_paid → paid, idempotent replay of payment_ref; J185 whatsapp capture → OCR fields → confirm; J186 insufficient funds honesty + overdue sweep flips status + digest includes overdue bills.

## Coder B — branch w31/scheduled-batch (migration 0102, journeys J187-J190)
**Payment scheduling + batch payments.** New server/services/scheduledPayments.ts + router server/routers/payments2.ts (name: `scheduledPayments`):
- Migration 0102: `scheduled_payments` (id uuid pk, tenant_id, kind varchar(16) [vendor_bill|payout|adhoc], target_id, recipient jsonb, amount_cents bigint, currency, execute_at timestamptz, status varchar(16) default 'pending' [pending|claimed|executed|failed|cancelled|insufficient_funds], idempotency_key unique, attempts int default 0, last_error, created_by, created_at, updated_at) + index (status,execute_at).
- schedule/list/cancel procedures. Execution engine: new `/api/scheduled/execute-payments` cron route in _core/index.ts (banner `=== W31 scheduled payments ===`; register in services/scheduler allowlist + k8s cron-scheduler.yaml as 36th CronJob, every 5 min): claim-before-send (guarded UPDATE pending→claimed WHERE execute_at<=now), then execute via the locked wallet debit helper; success → executed + wallet_tx ref `sched:<id>`; insufficient balance → status insufficient_funds (honest, retryable by merchant top-up: new `retry` procedure resets to pending); other failure → failed with last_error, attempts++, retry with backoff, dead-letter after 5. Never claim money moved before the ledger write commits.
- Batch payments: `batchPay` procedure — array of targets (vendor bills and/or ad-hoc recipients, max 50), single confirmation; per-item idempotency key `batch:<batchId>:<idx>`; each item independently executed in its own tx (partial failure isolation); returns per-item honest outcomes {executed|insufficient_funds|failed}; batch summary stored (`payment_batches` table in same migration 0102: id, tenant_id, total_cents, item_count, executed_count, failed_count, created_by, created_at).
- T-1 reminder: cron also sends WhatsApp reminder for bills/payments due within 24h (dedupe via metadata.remindedAt).
- Journeys: J187 schedule vendor-bill payment → cron executes at due time → wallet debited once, replay no-op; J188 insufficient funds at execution → honest state → top-up → retry → executed; J189 batch of 5 (incl. 1 insufficient) → 4 executed 1 honest failure, per-item idempotency; J190 T-1 reminder dedupe.

## Coder C — branch w31/approvals (migration 0103, journeys J191-J192)
**Threshold approval workflows.** New server/services/approvals.ts + router server/routers/approvals.ts:
- Migration 0103: `approval_requests` (id uuid pk, tenant_id, kind varchar(24) [vendor_bill_payment|scheduled_payment|withdrawal|payout], target_id, amount_cents bigint, currency, requested_by, approver_role varchar(16) default 'owner', status varchar(16) default 'pending' [pending|approved|rejected|expired|executed], decided_by, decided_at, decision_note, step_up_challenge_id, expires_at, executed_at, metadata jsonb, created_at) + index (tenant_id,status); `tenant_approval_policies` (tenant_id pk, threshold_cents bigint default 0 [0=off], kinds text[], approver_role, expiry_hours int default 72, updated_at, updated_by).
- Policy procedures (owner-only setPolicy; honest threshold semantics). Engine: exported `requireApprovalIfNeeded(tenantId, kind, amountCents, targetId, db)` — if policy threshold > 0 and amount >= threshold: create approval_requests row, return {approvalRequired:true, approvalId}; callers surface honest `pending_approval` state.
- Wire-in points (banner-commented, minimal edits): vendorBills.recordPayment (coordinate with A's payment_ref flow — if approval required, bill goes status 'approved' only after approval executes; A owns the file, so C provides the helper + contract and A calls it — C: document the contract in your report; merger wires if branches diverge), scheduledPayments execution for adhoc payouts above threshold, escrow.requestWithdrawal (insert check BEFORE step-up; existing step-up still applies — layered honestly).
- approve/reject procedures: owner/operator per policy.approver_role; approve of payout-kind above WITHDRAWAL_STEPUP_THRESHOLD still requires step-up (compose, don't bypass); approval execution performs the actual payment via the same locked helpers (approval is a gate, not a money mover — the original procedure executes on approval); single-consumption guarded UPDATE (concurrent approve → CONFLICT); expiry sweep (cron in existing execute-payments route or own block, banner).
- WhatsApp notification to approver on new request + approval result to requester.
- Journeys: J191 policy set → bill payment above threshold → pending_approval → owner approves in WA/dashboard → payment executes once (wallet asserted) → second approve CONFLICT; J192 reject → nothing moves + audit; expiry → expired + requester notified.

## Coder D — branch w31/ar-invoices (migrations 0104-0105, journeys J193-J196)
**AR invoices with payment links + reminders.** Extend (do NOT break) existing invoice router (commission invoices keep working) — new router server/routers/arInvoices.ts preferred:
- Migration 0104: `ar_invoices` (id uuid pk, tenant_id, customer_name, customer_phone, customer_email, invoice_no (tenant-scoped sequence), description, amount_cents bigint, paid_cents bigint default 0, currency, due_date, status varchar(16) default 'draft' [draft|sent|viewed|partially_paid|paid|overdue|cancelled], payment_link_ref unique, psp_reference, sent_at, viewed_at, paid_at, reminder_count int default 0, last_reminder_at, metadata jsonb, created_at, updated_at) + unique (tenant_id, invoice_no). Migration 0105: `ar_invoice_payments` (id, invoice_id, amount_cents, psp_reference unique, status, recorded_at).
- Procedures: create (draft), send → generates PSP payment link via existing provider layer (initiateWithFallback/payment links — reuse what receipts/payment.ts uses; sim uses metaMock), sets sent + WA message to customer with link; list/get with aging buckets; recordPayment webhook-path: on PSP payment success (reuse existing payment verification/confirm pipeline, reference = payment_link_ref) → ar_invoice_payments row + paid_cents increment + status partially_paid/paid — NEVER mark paid without verified provider status (post-W30 doctrine); cancel; overdue sweep + polite WA reminders (max 3, spacing 3d, dedupe via last_reminder_at) in own cron block (banner `=== W31 AR reminders ===`, add to scheduler allowlist + k8s as 37th CronJob, daily).
- Public payment page/link handling must follow publicProcedure exemplar (tracking.ts) — no tenant data leakage beyond the invoice itself.
- Journeys: J193 create→send→customer pays full via link→paid (wallet/ledger asserted through existing rails); J194 partial payment→partially_paid→remainder→paid, replay of webhook no-op; J195 overdue→reminder cadence (3 max)→stop; J196 cancel unpaid→link invalidated honestly.

## Merger notes (lead/merger executes after all 4 report)
Order: A → B → C → D. Journal idx 100-105 cumulative from 0099 tip. Runner union by W31 banners; simulation.test.ts count = 182 + 14 = 196 (toBe(196)). _core/index.ts: B owns execute-payments block, D owns AR reminders block, C adds expiry sweep inside B's block or own — union by banner. schema.ts/env.example/runner: append-only banners. escrow.ts requestWithdrawal: C's minimal insert only. trpc.ts: untouched unless C needs a helper (banner). vendorBills.ts A-owned; C's approval contract call inserted by merger if needed. Full gate: tsc 0 (6GB), full vitest excl sim, 196/196 journeys, authz green, pin intact, lockfiles clean. Then push master to w31-share, rsync to /mnt/agents/output/w31/merged-tree/, and lead publishes via Git Data API (BASE_SHA 3f7a0c02aa4e08dff1620abd1a7e7234358f238f, branch wave31-ap-ar, PR, squash, byte-identical verify).
