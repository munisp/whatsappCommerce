apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - network-policy.yaml
  - network-policies.yaml
  - apisix.yaml
  - ingress.yaml
  - api-gateway.yaml
  - platform.yaml
  - commerce-engine.yaml
  - ai-agent.yaml
  - ml-inference-deployment.yaml
  - ledger-bridge.yaml
  - tigerbeetle.yaml
  - recon-worker.yaml
  - temporal-deployment.yaml
  - temporal-worker.yaml
  - permify-deployment.yaml
  - keycloak.yaml
  - postgres.yaml
  - redis.yaml
  - hermes-router.yaml
  - hermes-bridge.yaml
  - cron-scheduler.yaml
  # Cluster-wide observability (OTel Collector, Prometheus, Grafana +
  # dashboards, Alertmanager, Jaeger, blackbox-exporter, kube-state-metrics,
  # Loki/Promtail, cluster-report) moved out of this repo on 2026-09-14 -
  # none of it was whatsapp-specific, it monitored the whole cluster. Now
  # lives in github.com/AfroNG/monitoring_dashboard, applied by Flux
  # (GitRepository + Kustomization `monitoring-dashboard` in flux-system).
  # === W39 durability (Coder A): PVC-backed backups, CronJobs #42/#43 ===
  - backups.yaml
  # === END W39 durability ===
  # monitoring/ requires the Prometheus Operator CRDs (ServiceMonitor,
  # PrometheusRule), which don't exist in this cluster - dead, never
  # actually applied. Not migrated; a candidate for deletion.
  #   kubectl apply -f k8s/monitoring/
