# W30 (V3#3) — CronJobs invoking the platform /api/scheduled/* routes on their
# documented cadences, using the scheduler image in --once mode with cron JWT
# auth (CRON_JWT from platform-secrets). The route allowlist + cadences are the
# single source of truth in services/scheduler/scheduler.mjs (parity asserted
# by journey J178); this manifest is generated from that table.

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-cart-recovery
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/cart-recovery"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-bookkeeping-digests
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/bookkeeping-digests"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-odoo-sync
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/odoo-sync"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-credit-loan-repayment
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/credit-loan-repayment"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-wa-send-retry
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/wa-send-retry"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-inventory-sync
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/inventory-sync"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-reconciliation-alert
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/reconciliation-alert"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-forecast-snapshot
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/forecast-snapshot"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-leaderboard-top3
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/leaderboard-top3"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-escrow-auto-confirm
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/escrow-auto-confirm"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-float-income
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/float-income"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-sla-scan
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/sla-scan"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-broadcast-scheduler
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/broadcast-scheduler"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-journey-tick
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/journey-tick"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-journey-orchestrate-tick
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/journey-orchestrate-tick"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-lead-model-tick
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/lead-model-tick"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-pd-model-tick
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/pd-model-tick"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-uplift-model-tick
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/uplift-model-tick"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-bandit-reward-tick
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/bandit-reward-tick"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-broadcast-dispatch
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/broadcast-dispatch"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-window-expiry-check
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/window-expiry-check"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-wa-quality-refresh
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/wa-quality-refresh"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-wa-media-download
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/wa-media-download"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-wa-webhook-retry
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/2 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/wa-webhook-retry"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-inventory-reservation-sweep
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/inventory-reservation-sweep"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-integration-outbox-dispatch
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/integration-outbox-dispatch"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-odoo-inventory-sync
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/10 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/odoo-inventory-sync"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-medusa-catalog-sync
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/30 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/medusa-catalog-sync"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-nightly-finetune
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/nightly-finetune"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-ab-test-metrics
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/30 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/ab-test-metrics"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-drift-alert
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 */6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/drift-alert"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-delivery-summary
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/delivery-summary"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-hermes-po-expiry
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/hermes-po-expiry"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-hermes-health-snapshot
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/hermes-health-snapshot"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-generate-invoices
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 0 1 * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/generate-invoices"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
---

---
# === W31 scheduled payments === 36th CronJob: claim-before-send execution of
# scheduled_payments + T-1 WhatsApp reminders (every 5 min). Cadence mirrors
# services/scheduler/scheduler.mjs (parity asserted by J178).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-execute-payments
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/execute-payments"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
---
# === W31 approvals (Coder C) — expiry sweep for pending approval requests ===
# NOTE for merger: Coder B's execute-payments CronJob is the 36th on the
# merged branch; this is the 37th. If both exist, keep both (distinct names).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-approvals-expiry
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 */15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/approvals-expiry"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
---
# === W31 AR reminders === (36th CronJob on this branch; spec numbering: D is
# 38th only if B=36/C=37 land first — merger resolves sequential numbering)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-ar-reminders
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 0 9 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/ar-reminders"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
---
# === W32 installment due === 39th CronJob: daily pay-over-time installment
# capture via the existing mandate rails + honest overdue dunning (see
# server/services/payOverTime.ts). Cadence mirrors
# services/scheduler/scheduler.mjs (parity asserted by J178).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-installment-due
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 0 8 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/installment-due"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
# === END W32 installment due ===
---
# === W32 recurring === (Coder B) — daily recurring bills / auto-pay sweep.
# NOTE for merger: on this standalone branch this is the 39th CronJob; per
# SPEC_W32 Coder A's installment-due cron takes #39 and this is #40 on the
# merged branch — renumber in this comment only if needed (names are stable).
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-recurring-run
  namespace: whatsapp-commerce
  labels:
    app: cron-scheduler
spec:
  schedule: "0 7 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
          containers:
            - name: scheduler
              image: whatsapp-commerce/scheduler:latest
              args: ["node", "scheduler.mjs", "--once", "/api/scheduled/recurring-run"]
              env:
                - name: PLATFORM_URL
                  value: "http://platform.whatsapp-commerce.svc.cluster.local:3000"
                - name: CRON_JWT
                  valueFrom:
                    secretKeyRef:
                      name: platform-secrets
                      key: CRON_JWT
              resources:
                requests:
                  cpu: "10m"
                  memory: "32Mi"
                limits:
                  cpu: "100m"
                  memory: "128Mi"
