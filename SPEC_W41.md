# SPEC_W41 — Use-case cluster: buyer installments, wallet, split pay, returns, multi-currency, tokenized methods

Base: main @ 90b3a27f9fb43d4ce5a4f890586043f57da374d6 (post-W40). BINDING. Sources: /mnt/agents/output/w36/audit-usecases.md (UC-1,2,3,4,5,6 + relevant P1s), audit-orders.md (ORD-6≡UC-4 RMA), p0-verification.md notes.

## Global invariants
- paymentConfirm.ts PINNED (md5 2f77ea4816d1adc5cb35473bd35d1697) — adjacent seams only.
- Integer cents everywhere; multi-currency DISPLAY is presentational only — ledger stays NGN kobo (honest doctrine: no multi-currency ledger this wave).
- Additive-only schema; hand-written migrations 0127+ (journal after 0126). Lockfiles FROZEN. Banner edits. Fail-closed on money ambiguity; verify-first on PSP.
- Reuse W32 installment engine patterns (merchant-AP payOverTime) for buyer side where semantics allow — read payOverTime.ts first, do not fork blindly.
- Journeys: A=J286–J292, B=J293–J299, C=J300–J306 (register; merger owns count). Fast-fail after 2 attempts; push incrementally.

## Coder A — buyer installments + tokenized customer payment methods (UC-1, UC-6)
1. buyer_installment_plans (mig 0127): order-linked plans (order_id, tenant, buyer phone/customer ref, total, down_payment, N installments, schedule, status); eligibility: merchant opt-in per tenant (config flag) + order total threshold; checkout offer via WhatsApp/Telegram interactive (parity: both channels via channelSender/notifyCustomer); first charge = down payment at order confirm (adjacent seam in checkout caller, NOT paymentConfirm.ts); subsequent charges via existing mandate/PSP charge paths with pot_charges-style durable ledger (reuse W38 patterns: durable pending rows, verify-first sweep, exactly-once); failure -> dunning via existing dunning; paid-in-full -> order released to fulfillment (gate shipment on plan status for COD-like exposure).
2. Tokenized customer payment methods (UC-6): reusable PSP authorization tokens (Paystack reusable_authorization / flutterwave token): save after successful charge WITH explicit buyer consent (WA/TG confirm prompt); stored encrypted v1:; buyer can list/revoke via chat ("my cards"); used for installment charges + one-tap repurchase. Migration same 0127 (customer_payment_tokens table). NEVER store PAN. Journeys: plan creation + schedule, down payment at confirm, installment charge exactly-once incl. timeout-verify, dunning on failure, token save/consent/revoke, one-tap reorder with token.
Branch w41/buyer-credit.

## Coder B — customer wallet + split payments (UC-2, UC-3)
1. customer_wallets (mig 0128): per-tenant per-customer store credit (balance_cents, currency NGN); credit sources: refund-to-wallet (buyer chooses wallet instead of PSP reversal — seam in refund flow, adjacent), merchant goodwill adjustment (audited admin action), overpayment auto-credit (from PAY underpayment/overpayment handling — read existing); spend: apply at checkout before PSP (partial allowed: wallet first, remainder PSP); ledger: append-only customer_wallet_entries (double-entry style reference to order/refund); never negative (claim-first FOR UPDATE); expiry optional per tenant. Refund-to-wallet must respect W38 cumulative caps (wallet credit counts toward refunded total).
2. Split payments / group contributions (UC-3): split_payment_sessions (order_id, target amount, N participants, invited phones, per-share computed); each participant pays via payment link (PSP) or wallet; order confirms only when fully funded (claim-first tally); timeout -> auto-refund contributors (wallet credit default, PSP reversal optional); expiry 48h default; WhatsApp/Telegram invite messages via notifyCustomer. Journeys: wallet credit/spend/partial, no negative balance under race, split session full funding -> confirm, timeout -> refunds, overpayment -> wallet credit.
Branch w41/wallet-split.

## Coder C — RMA/returns + multi-currency display (UC-4≡ORD-6, UC-5)
1. RMA lifecycle (mig 0129): rma_requests (order_id, tenant, buyer, items jsonb qtys, reason, evidence_media_id, status requested|approved|rejected|received|restocked|refunded|closed, timestamps); buyer initiates via WA/TG ("return order X"); merchant approve/reject in admin or WA merchant command; approval -> return instructions message; received -> restock via unified orderCancel/inventory path (reuse W38 releaseCommitted/restock helpers — read them); refunded -> W38 refund path (caps, idempotency, clawback) OR wallet credit (coordinate contract with Coder B: refund_to_wallet flag — binding seam); escrow interplay: pause release while RMA open (read escrow state machine; honest block). Status notifications both channels.
2. Multi-currency display (UC-5): tenant config display_currency + static rate table (mig 0129 additive jsonb or reuse fx_quotes rates — read fxQuotes service; display-only conversion helper formatPrice(tenant, amountKobo) -> "₦1,500 (~$1.00)"); catalog/product messages, order summary, payment link text show dual display; PSP charge stays NGN (honest note in message footer). Rate source: manual tenant-set with last_updated (no live feed this wave — documented). Journeys: RMA full lifecycle incl. restock + refund-cap respect, escrow pause, dual-currency rendering in catalog + order summary, NGN charge unchanged.
Branch w41/rma-fx.

## Merger (reuse warm coder)
Order A→B→C. Journal 0127→0128→0129 re-chain. Binding seam: B's wallet used by A (token one-tap + plan charges optional wallet) and C (refund_to_wallet) — contracts: B exports creditWallet(tenant, customerRef, amountCents, reason, refId), debitWallet(...), walletBalance(...) claim-first; merger verifies A/C call sites compile against B's real module (no stubs left — grep TEMP STUB = 0). Journey count = actual. Full gate: tsc 0 (8GB) / vitest 0 fail / sim N+N / authz / pin / lockfiles unchanged / no empties / no markers. merge-log + merged tree; lead fast-publishes.
