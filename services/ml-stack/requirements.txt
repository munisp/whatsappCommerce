# ─── ML Inference Stack — CPU-only dependencies ──────────────────────────────
# Install CPU wheels of torch (no CUDA) with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# The Dockerfile already passes this index for the torch install.

# Web framework / ASGI server
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.6

# HTTP client (platform callbacks)
httpx==0.28.1

# Data / scientific
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0

# Database
psycopg2-binary==2.9.10

# ML runtimes (CPU)
# NOTE: resolves to the +cpu wheel when the PyTorch CPU index above is passed.
torch==2.5.1
onnxruntime==1.20.1
scikit-learn==1.6.0

# === W35 otel-ml-stack === fail-open OTel (lazy; loaded only when OTEL_ENABLED=true)
opentelemetry-sdk==1.37.0
opentelemetry-exporter-otlp-proto-http==1.37.0
